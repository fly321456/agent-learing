# Course Checkpoints

These snapshots support the 32-lesson course. Copy the previous checkpoint to
`.learning/current`, complete four lessons there, then compare with the next
checkpoint. Checkpoints are read-only reference states, not public package APIs.

Every checkpoint runs offline with `python demo.py`.

