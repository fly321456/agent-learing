# 03 Agent Loop

单文件离线 Agent，展示 while Loop、Tool Registry、同轮多 Tool、结构化错误和明确结束原因。

```powershell
python demo.py
```

四个逐课步骤位于 `steps/l09_*.py` 至 `steps/l12_*.py`。Expected: `tool_results=2` and `finish_reason=completed`.
