# 第七课学习整理：写第一行 Agent 代码

## 本课定位

从这一课开始，正式进入编码阶段。

但这节课依然非常克制，目标只有一个：

> 成功调用一次 LLM

这看起来很基础，但它其实是所有 Agent 的起点。

同时，这一课还做了一次路线纠偏：

> 后续主线采用 `OpenAI Responses API + OpenAI Agents SDK + MCP`

而不是一开始就围绕 LangChain 展开。

这代表的是一种更贴近 2026 年主流 Agent 工程实践的学习路径。

---

## 为什么要更新技术路线

课程特别强调了一点：

> 不教已经过时的 Agent 开发方式，而直接使用当前主流接口和工程模型。

所以后续重点会放在：

- `OpenAI Responses API`
- `OpenAI Agents SDK`
- `MCP`

这是因为现代 Agent 开发早就不只是“提示词 + 函数调用”这么简单，而是在逐步走向：

- 结构化事件输出
- 更明确的运行时抽象
- 可扩展的工具协议

因此，从一开始就用更现代的接口心智模型，会比学一堆老式封装更稳。

---

## 本课目标：成功调用 LLM

第七课只做一件事：

> 证明本地环境可以成功通过 `Responses API` 调用模型

这一步完成后，就意味着后面所有 Agent 开发的基础都打通了：

- API Key 可用
- SDK 安装正确
- `responses.create()` 可用
- 模型能正常返回 Response

---

## 第一步：初始化 Client

`config.py`

```python
import os
from openai import OpenAI

client = OpenAI(
    api_key=os.getenv("OPENAI_API_KEY")
)
```

代码很少，但意义很明确：

- 配置与业务逻辑分离
- 后续所有模型调用都复用同一个 client

这一步看起来像普通 LLM 应用，其实也确实如此。

因为：

> Agent 本质上就是建立在 LLM 之上的更复杂控制系统

没有最基本的 LLM 调用能力，就谈不上后续的 Tool、Loop、Memory。

---

## 第二步：第一次调用 `responses.create()`

`main.py`

```python
from config import client

response = client.responses.create(
    model="gpt-5",
    input="你好，请介绍一下自己。"
)

print(response.output_text)
```

如果这段代码运行成功，就至少说明了三件事：

- API Key 正确
- OpenAI SDK 正常
- Responses API 调用链路正常

这虽然还不是 Agent，但它是 Agent 的起点。

---

## 这节课真正重要的地方：不要只看 `output_text`

很多教程会在这里结束，然后默认你以后就一直用：

```python
print(response.output_text)
```

但这只是普通 LLM 应用的读法，不是 Agent 工程师的读法。

课程特别强调应该尽快养成一个工程习惯：

```python
print(response)
```

或者：

```python
print(response.output)
```

原因很简单：

> 对 Agent 来说，最重要的信息往往不在 `output_text`，而在 `output`

---

## Response 的基本结构

可以把 Response 理解成一个更完整的响应对象：

```text
Response

├── id
├── model
├── usage
├── output
└── output_text
```

其中：

- `output_text` 是最终可读文本的便捷入口
- `output` 才是 Agent 开发最核心的数据结构

因为在未来的 Agent 场景里，`output` 里出现的可能不只是 message，还可能包括：

- `message`
- `function_call`
- `reasoning`
- 其他动作型输出

这意味着：

> Agent 面对的不是一句回答，而是一系列结构化事件

---

## 为什么 Response 要这样设计

这一点需要从 Agent 的真实运行方式来理解。

例如用户说：

> 帮我分析 GitHub 项目

模型的第一轮返回，可能不是“项目分析如下”，而是一些中间动作：

### Event 1

```text
Reasoning
```

例如：

> 我应该先下载仓库

### Event 2

```text
Function Call
```

例如：

```text
git_clone()
```

然后程序执行工具，再把结果回传给模型。

接着模型下一轮又可能产生新的事件：

### Event 3

```text
Reasoning
```

例如：

> 我应该读取 README

### Event 4

```text
Function Call
```

例如：

```text
read_file()
```

所以现代 Agent 中的 Response，本质上更像：

> 事件流（Event Stream）

而不是单一文本回复。

---

## 从“GPT 回复了我”到“GPT 返回了一系列 Event”

这是这一课要建立的新表达方式，也是新思维方式。

以后不要只说：

> GPT 回复了我

更准确的说法应该是：

> GPT 返回了一系列 Event

例如：

```text
Event1: Reasoning
Event2: Function Call
Event3: Message
```

而 Agent 程序真正做的事情，就是持续消费这些 Event，并根据 Event 类型决定下一步动作。

---

## Agent 工程师的第一个习惯

以后每次拿到 Response，第一件事不应该是：

```python
print(response.output_text)
```

而应该先看：

```python
for item in response.output:
    print(item.type)
```

这样做的价值在于：

- 先理解当前返回了什么类型的事件
- 再决定程序应该如何处理

这也是普通 LLM 开发者和 Agent 工程师之间非常明显的一个分水岭。

---

## 再次回到 Agent Loop

虽然这一课还没有正式开始写完整 Loop，但已经开始强调它的核心结构：

```python
while True:

    response = model()

    if response 是 Tool:
        execute()
        continue
    else:
        break
```

这里要特别注意：

真正决定一个系统是不是 Agent 的，不是：

```python
client.responses.create()
```

而是：

```python
while True
```

也就是说，模型调用只是能力接口，Loop 才是系统控制核心。

---

## Agent 不只是 Tool Calling，而是 State Machine

这一课还特别升级了一个更工程化的视角：

> Agent = State Machine（状态机）

可以把一个最小 Agent 流程理解成：

```text
START
↓
CALL_MODEL
↓
MODEL_RESPONSE
↓
TOOL_CALL ?
↓
YES
↓
EXECUTE_TOOL
↓
CALL_MODEL
↓
MODEL_RESPONSE
↓
NO
↓
FINISH
```

这本质上就是状态在不断流转：

- 开始状态
- 调模型状态
- 收到响应状态
- 判断是否需要工具
- 执行工具状态
- 再次调模型
- 最终结束状态

这也解释了为什么很多成熟框架会把 Agent 设计成图、节点、Runner 或状态流转系统。

例如 LangGraph 之所以叫 Graph，本质上就是因为：

> 它在用图结构表达 Agent 的状态机

---

## 本课背后的框架理解视角

课程已经开始引导进入“看框架不只看 API”的阶段。

未来分析框架时，会重点观察这些抽象为什么存在：

### OpenAI Agents SDK

- `Agent`
- `Runner`
- `Tool`
- `Session`

### Claude Code 一类系统

- Planner
- Executor
- Editor

### LangGraph

- Graph
- Node
- Edge
- State

这些名字表面不同，但底层都在解决同一个核心问题：

> 如何优雅地实现 Agent Loop

---

## 课程节奏升级

这一课还给出了后续更高效的学习节奏：

- 10 分钟理论
- 40 分钟实战
- 10 分钟源码分析

并且课程不再做零散示例，而是持续构建一个完整项目：

```text
agent-course/
│
├── app.py
├── agent.py
├── tools.py
├── prompts.py
├── config.py
├── memory.py
└── requirements.txt
```

这个项目会随着课程不断扩展，最终演进成一个具备：

- Tool
- Memory
- RAG
- MCP

等能力的完整 Agent。

---

## 本课核心结论

### 1. 第一个目标不是写复杂 Agent，而是先成功调用 LLM

所有复杂系统都从这个最小成功开始。

### 2. `response.output_text` 不是 Agent 开发的核心

真正关键的是 `response.output`。

### 3. 现代 Response 更像事件集合

它里面可能包含 message、reasoning、function_call 等多种输出项。

### 4. Agent 的本质不只是 Tool Calling

更准确地说，它是一个状态机。

### 5. `while True` 比单次 `responses.create()` 更能代表 Agent 核心

Loop 决定了它是不是 Agent。

---

## 下一步

下一课将正式进入小步快跑式编码：

1. 初始化 `OpenAI` Client
2. 调用 `responses.create()`
3. 打印 `response.output`
4. 区分 `message` 与 `function_call`
5. 开始写最小事件处理逻辑

从那时开始，就会真正把“事件驱动 + 状态机”的思想落实到代码中。

