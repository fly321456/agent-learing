# Sprint 1 - Lesson 5 面试题整理

## 题目 1

**问题：** 为什么在支持多个 Tool 之后，还需要继续从 Tool Registry 升级到 ToolManager？

**参考答案：** 因为简单的 Tool Registry 通常只解决“按名字找到函数”的问题，但真实 Agent 系统还需要统一管理 Tool Schema、Tool 执行、参数传递和注册流程。如果这些职责继续散落在 Runner 周边，Runner 就会逐渐承担过多工具相关逻辑。升级到 ToolManager，本质上是在把工具系统收敛成独立模块，让运行时调度和工具管理各自负责自己的边界。

**追问方向：** Registry 和 Manager 在职责上最大的区别是什么？

**易错点：** 觉得 ToolManager 只是“多写了一个类”，没有看到职责收拢的价值。

## 题目 2

**问题：** 一个最小 ToolManager 至少应该负责哪些事情？

**参考答案：** 一个最小 ToolManager 至少应该负责三件事：注册工具、提供所有 Tool Schema 给 LLM，以及根据 tool name 和 arguments 执行对应工具。也就是说，它不仅要知道有哪些 Tool，还要能把这些 Tool 以模型可理解的方式暴露出去，并在运行时真正协调执行。这三项职责一旦集中起来，Runner 就能保持轻量。

**追问方向：** 如果后续再扩展，ToolManager 最可能新增哪些职责？

**易错点：** 只回答“负责执行 Tool”，忽略 Schema 和注册机制。

## 题目 3

**问题：** 为什么说 ToolManager 的出现是架构升级，而不只是代码重构？

**参考答案：** 因为它改变的不是代码摆放位置，而是职责边界。引入 ToolManager 后，Runner 不再直接管理工具函数、Schema 列表和工具执行细节，而是只负责调度流程；工具相关逻辑被集中在专门模块中。这种变化会直接影响系统后续的扩展方式、维护成本和模块耦合关系，因此它属于架构层面的升级，而不是简单重构。

**追问方向：** 哪些信号说明一个项目已经到了必须抽 ToolManager 的阶段？

**易错点：** 把职责拆分和“代码目录调整”混为一谈。

## 题目 4

**问题：** 为什么 Runner 应该调用 `tool_manager.execute(...)`，而不是自己直接操作 Tool 字典？

**参考答案：** 因为 Runner 的核心职责是驱动运行流程，而不是理解和管理工具系统。如果 Runner 直接操作 Tool 字典，它就必须知道工具注册细节、Schema 来源和参数分发方式，职责会被不断侵蚀。通过调用 `tool_manager.execute(...)`，Runner 只表达“现在我要执行一个工具”，而如何查找和执行交给 ToolManager 负责，这样模块边界更清晰。

**追问方向：** 这种做法如何帮助未来做测试和替换实现？

**易错点：** 认为直接访问字典更简单，所以就一定是更好的设计。

## 题目 5

**问题：** 为什么 `ToolManager.execute()` 从第一版开始就要考虑无参 Tool 和有参 Tool？

**参考答案：** 因为这两类调用形态会直接影响工具分发逻辑的通用性。像 `get_current_time()` 这样的无参 Tool 和 `calculator(expression)` 这样的有参 Tool，是后续大多数 Tool 的两种基本形态。如果第一版执行器不把这两类情况处理清楚，后面加入 `read_file(path)`、`search(query)` 等工具时就会不断打补丁。因此一开始就兼顾空参数与字典参数，是为后续可扩展性打基础。

**追问方向：** 再往后如果引入更复杂嵌套参数结构，ToolManager 可能要怎么升级？

**易错点：** 觉得当前 Tool 简单，所以参数处理可以先随便写。

## 题目 6

**问题：** Sprint 1 - Lesson 5 的核心升级点，相比上一节到底多了什么？

**参考答案：** 上一节的重点是让 Agent 跑通多 Tool 的闭环，而这一节的重点是把“多 Tool 能跑”进一步升级成“多 Tool 的职责边界清晰”。也就是说，本节并不只是继续增加能力，而是在把工具系统正式抽成独立模块，让 Runner 更像纯运行时调度器。这标志着项目开始从功能实现走向更稳定的框架化设计。

**追问方向：** 为什么说这一节是从“能扩展”走向“能持续扩展”的关键一步？

**易错点：** 把这一节理解成只是把字典换成类，没有看到结构变化。

