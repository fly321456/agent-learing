# Sprint 1 - Lesson 1 面试题整理

## 题目 1

**问题：** 为什么第一版 Agent 不应该把 `run()` 直接写进 `Agent` 类里？

**参考答案：** 因为 `Agent` 更适合承载静态配置，例如模型、指令和工具集合；而真正的运行过程，包括调用模型、处理 Tool Call、执行 Loop 和控制生命周期，更适合由 `Runner` 负责。如果把 `run()` 直接塞进 `Agent`，会把配置层和运行时层耦合在一起，后续系统扩展和职责管理都会变差。

**追问方向：** 如果未来要支持多个不同 Agent，共用同一套运行逻辑，这种拆分会带来什么好处？

**易错点：** 觉得 `agent.run()` 更直观，就忽略了架构边界问题。

## 题目 2

**问题：** 为什么 `Runner.run(agent, user_input)` 比 `Runner` 在内部固定持有一个 `agent` 更灵活？

**参考答案：** 因为这样设计意味着一个 `Runner` 可以运行多个不同的 Agent，而不是和某一个具体 Agent 强绑定。这样更利于复用运行时逻辑，也更方便后续扩展到多 Agent、不同角色 Agent，或者同一 Runner 驱动不同任务场景。这体现了更好的解耦和复用性。

**追问方向：** 如果以后加入 `ReviewAgent`、`SearchAgent`、`CodeAgent`，这一设计会如何简化系统组织？

**易错点：** 把参数传递方式只看成代码风格问题，没有看到复用性差异。

## 题目 3

**问题：** 为什么项目一开始就要定义 `BaseLLM` 抽象接口，而不是直接在业务代码里写 OpenAI 调用？

**参考答案：** 因为上层 Agent 系统真正需要的是“生成响应”的稳定能力，而不是某家模型 SDK 的具体写法。先定义 `BaseLLM.generate()`，可以把 OpenAI、Claude、Gemini、Qwen 等底层实现藏在统一接口之后，降低系统和具体供应商之间的耦合。这是依赖倒置在 Agent 项目中的典型体现。

**追问方向：** 如果后续需要切换模型供应商，没有 `BaseLLM` 抽象会带来哪些重构成本？

**易错点：** 觉得“现在先直接写 OpenAI，后面再抽象”总是低成本。

## 题目 4

**问题：** 这一节里最核心的软件工程原则是什么？

**参考答案：** 最核心的是高内聚、低耦合和单一职责。Agent 只保存 Agent 的身份和配置，Runner 只负责运行，BaseLLM 只定义模型接口。通过一开始就控制好职责边界，可以避免后续把工具、记忆、重试、日志、模型调用等逻辑全部堆到一个类或文件里，从而保持系统可扩展、可维护。

**追问方向：** 在 Agent 项目里，最容易最先失控成“上帝对象”的通常是哪一层？

**易错点：** 只把这些原则当成理论口号，没有理解它们为什么会直接影响后续代码质量。

## 题目 5

**问题：** 为什么说这一节虽然代码很少，但它是整个 Agent Framework 的架构地基？

**参考答案：** 因为这一节完成的是最关键的边界划分：谁负责配置，谁负责运行，谁负责模型抽象。后续无论增加 Tool Calling、Memory、Session、Tracing 还是多 Agent，这些能力都必须建立在当前这套职责拆分之上。如果这一步边界没划清楚，后面的每一次扩展都会变成打补丁式开发。

**追问方向：** 如果这一节直接为了“快点跑起来”把所有逻辑写在一个文件里，后面最可能在哪些地方出问题？

**易错点：** 误以为“现在只是搭骨架，不重要”，没有看到骨架决定上限。

## 题目 6

**问题：** 为什么这一节的 Git Commit 更像“架构提交”而不是“功能提交”？

**参考答案：** 因为这一节的重点不是完成某个复杂功能，而是确定后续项目的结构边界和演进方向。像 `Initialize Agent Architecture` 这样的提交，真正沉淀的是职责拆分、抽象层次和模块组织方式。功能以后可以不断补，但如果基础架构方向错了，后面功能越多，重构成本越高。

**追问方向：** 在真实团队中，为什么这类“先立结构再加功能”的提交很有价值？

**易错点：** 只用“代码量多少”衡量提交价值，而忽略架构决策本身就是重要产出。

