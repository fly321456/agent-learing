# Sprint 1 - Lesson 4 面试题整理

## 题目 1

**问题：** 为什么当 Agent 开始支持多个 Tool 时，不能继续把工具执行逻辑写死在 Runner 里？

**参考答案：** 因为一旦工具数量增加，Runner 如果继续通过 `if/elif` 手写每个 Tool 的执行分支，就会同时承担运行时控制、工具路由和工具细节管理等多重职责。这样每增加一个 Tool 都需要修改 Runner，导致 Runner 越来越臃肿，也破坏了可扩展性。更合理的做法是引入 Tool Registry 或 Tool Manager，让 Runner 只负责调度，不直接依赖每个工具的具体实现细节。

**追问方向：** 如果后续 Tool 从 2 个增长到 20 个，不做抽象会出现哪些具体问题？

**易错点：** 只看现在工具少，觉得手写分支更“直接”，忽略了扩展成本。

## 题目 2

**问题：** Tool Manager 或 Tool Registry 出现的根本原因是什么？

**参考答案：** 根本原因是工具系统一旦超过极小规模，就需要有独立模块负责工具的注册、保存、查找和执行。否则这些职责就会泄漏到 Runner 或其他核心模块里，导致系统边界混乱。Tool Manager 的本质不是为了“加一层封装”，而是为了把工具相关逻辑集中收敛，让运行时逻辑和工具管理逻辑分离。

**追问方向：** 在最小实现阶段，为什么可以先用字典模拟 Tool Registry？

**易错点：** 把 Tool Manager 理解成过度设计，而不是规模化后的必需抽象。

## 题目 3

**问题：** 为什么说一个 Tool 在工程上至少包含“函数实现”和“Schema 描述”两层？

**参考答案：** 因为函数实现负责真正执行动作，例如计算、读文件或获取时间；而 Schema 负责向 LLM 描述这个工具的名称、用途和参数结构。模型只能根据 Schema 来决定是否调用工具，而 Python 运行时则真正执行函数。所以一个 Tool 不只是一个函数，也不只是一个配置，而是执行层和描述层共同组成的能力单元。

**追问方向：** 如果只有函数没有 Schema，或者只有 Schema 没有函数，各会缺失什么能力？

**易错点：** 认为 Tool 就等于一个 Python 函数。

## 题目 4

**问题：** 为什么在这一节里引入 `calculator(expression)` 是一个很好的教学选择？

**参考答案：** 因为它和 `get_current_time()` 形成了很好的对照：前者是有参 Tool，后者是无参 Tool。通过同时支持这两类工具，系统会第一次面对参数解析、Schema 差异以及基于参数执行 Tool 的问题。这让学习者可以更具体地理解 Tool Calling 不只是“调用函数”，还涉及参数结构化和分发机制。

**追问方向：** 再往下增加第三个 Tool，优先选择 `read_file` 还是 `search`，为什么？

**易错点：** 只关注功能本身，忽略这个 Tool 对架构训练的代表性。

## 题目 5

**问题：** 为什么 `tool_registry[tool_name]` 这种查表方式比一长串 `if/elif` 更接近框架设计？

**参考答案：** 因为查表方式把工具选择逻辑从控制流分支转成了数据驱动分发。Runner 不再需要显式知道每个 Tool 的名字和执行细节，而是通过统一的注册表按名字查找并执行。这样做不仅代码更简洁，也更容易扩展、测试和替换，是很多框架内部管理插件、命令或工具系统时常见的设计方式。

**追问方向：** 查表方式再往前一步，如何演化成真正的 Tool 对象系统？

**易错点：** 只看到写法简洁，没有看到它背后的解耦价值。

## 题目 6

**问题：** Sprint 1 - Lesson 4 标志着项目在哪个维度上的升级？

**参考答案：** 这一节标志着项目从“单工具可运行 Agent”升级为“可扩展的多工具 Agent 雏形”。重点不只是多加了一个 Tool，而是第一次正式引入工具注册和分发的工程思维，让 Runner 开始摆脱写死工具逻辑的方式。这是项目从“能跑”走向“能扩展”的重要节点，也是后续独立抽出 `ToolManager` 的前置步骤。

**追问方向：** 为什么说这一节比单纯增加一个新功能更像一次架构升级？

**易错点：** 把这一节理解成只是“多写了一个 calculator”。

