# Sprint 1 - Lesson 3 面试题整理

## 题目 1

**问题：** 为什么说一个程序只有在实现了 Loop 之后，才真正开始接近 Agent？

**参考答案：** 因为 Agent 的核心不是调用一次模型，而是围绕模型响应持续进行“判断 - 执行 - 再判断”的闭环。只有加入 Loop，系统才能根据模型返回决定是否调用 Tool、是否继续执行以及何时结束任务。没有 Loop，程序通常只是一次请求一次响应的 LLM 应用，而不是完整 Agent。

**追问方向：** 哪些看起来“用了 Tool”的程序，其实仍然不是 Agent？

**易错点：** 以为只要能调一次 Tool，就已经是 Agent。

## 题目 2

**问题：** 为什么 Tool Calling 至少通常包含两轮模型交互？

**参考答案：** 因为第一轮模型往往只是输出 Tool Call，也就是决策“应该调用哪个工具、传什么参数”；这时还没有真正生成面向用户的最终答案。只有在程序执行 Tool 并把 Tool Result 回传给模型后，模型才能基于真实结果生成最终自然语言回答。因此 Tool Calling 至少通常包含“模型决策一轮”和“模型总结一轮”。

**追问方向：** 在什么情况下，这个过程会扩展成三轮、四轮甚至更多轮？

**易错点：** 把第一次 `function_call` 当成最终答案。

## 题目 3

**问题：** 为什么 `while True` 应该放在 Runner 里，而不是放在 Agent 里？

**参考答案：** 因为 `while True` 代表的是运行时循环，是生命周期控制的一部分，而不是 Agent 的静态配置。Agent 更适合保存 LLM、Prompt、Tool 集合等能力描述；Runner 才负责驱动状态流转、执行 Tool、回传结果和结束循环。把 Loop 放在 Runner 中，可以保持配置层和运行时层清晰分离，符合单一职责原则。

**追问方向：** 如果把 Loop 塞进 Agent，会对后续扩展带来哪些问题？

**易错点：** 觉得“写在哪都一样”，忽略了运行时职责边界。

## 题目 4

**问题：** 为什么 `if "时间" in user_input` 这种写法不属于真正的 Agent 设计？

**参考答案：** 因为这种写法把决策逻辑写死在 Python 代码里，本质上是规则驱动的 Workflow，而不是模型驱动的 Agent。真正的 Agent 应该由 LLM 根据 Prompt、上下文和 Tool Schema 决定是否调用 Tool，Python 只负责执行。否则系统虽然表面上能完成任务，但并没有体现 Agent 的动态决策能力。

**追问方向：** 在实际工程中，哪些少量规则仍然可以保留在 Python 层？

**易错点：** 把“能完成任务”误认为“设计就是 Agent”。

## 题目 5

**问题：** 在最小 Agent Loop 中，Runner 最少需要承担哪些职责？

**参考答案：** Runner 最少需要负责：组织消息、调用 LLM、检查 `response.output` 是否包含 Tool Call、执行对应 Tool、把 Tool Result 回传给模型，以及在没有 Tool Call 时结束循环并返回最终答案。也就是说，它是模型决策与工具执行之间的真实桥梁，也是整个运行生命周期的控制中心。

**追问方向：** 如果以后加入 Retry、Tracing、Memory，这些能力为什么通常也会落到 Runner 周边？

**易错点：** 只把 Runner 看成“调一次模型的辅助类”。

## 题目 6

**问题：** 为什么说“模型提出调用请求，Runner 负责真正执行”是理解 Agent 的关键分工？

**参考答案：** 因为 LLM 本身不会执行 Python 函数、访问本地文件或运行 Shell，它只能输出结构化的调用意图，例如 function call。真正的执行必须由外部宿主程序完成，而在这个架构里，执行责任通常由 Runner 协调 Tool Manager 或具体 Tool 去完成。理解这个分工，才能真正分清模型决策层和系统执行层。

**追问方向：** 如果把这两层混在一起，最容易出现什么认知错误？

**易错点：** 误以为模型“写出了函数名”就等于它已经执行了工具。

