# 模块 02 验收与面试

## 原理验收

不用看代码，画出 user input、第一次 response.output、Python Tool、function_call_output 和第二次 output_text。说明 `call_id` 如何关联数据，以及为什么模型只提出行动、不执行函数。

## 项目验收

```powershell
python agent-from-scratch/course-checkpoints/02-tool-calling/steps/l05_messages_instructions_context.py
python agent-from-scratch/course-checkpoints/02-tool-calling/steps/l06_first_responses_text_call.py
python agent-from-scratch/course-checkpoints/02-tool-calling/steps/l07_tool_schema_and_function_call.py
python agent-from-scratch/course-checkpoints/02-tool-calling/steps/l08_execute_and_return_tool_output.py
python agent-from-scratch/course-checkpoints/02-tool-calling/demo.py
```

默认运行不得访问网络。最终 Demo 应显示 `requests=2`、原 call ID 和最终答案。

## 自动验证

```powershell
cd agent-from-scratch
python -m pytest -q tests/test_course_module2.py
```

## 故障注入

分别注入非法 arguments、未知工具名和错误 call ID。解释哪一层发现问题、handler 是否执行、错误如何进入第二次请求。恢复后重新运行测试。

## 代码讲解

从 `run_fixed_tool_round_trip` 的 input_items 开始，依次讲解第一次请求、保留 output、解析参数、路由处理器、追加 output 和第二次请求。必须指出它为什么还不能处理第三次 Tool 请求。

## 精选面试题

1. Instructions 为什么不应被视为永久 Session 状态？
2. `response.output_text` 为什么不能替代遍历 output？
3. JSON Schema 与业务安全校验有什么区别？
4. Function Calling 与函数执行的责任边界是什么？
5. 为什么默认自动测试必须离线？
6. call ID 错配会破坏哪条数据链？

## 通过标准

四个步骤和专项测试通过；能手绘两次调用；能解释三个故障；没有硬编码密钥或永久默认模型。通过后进入模块 3。

[返回模块导学](模块导学.md)
