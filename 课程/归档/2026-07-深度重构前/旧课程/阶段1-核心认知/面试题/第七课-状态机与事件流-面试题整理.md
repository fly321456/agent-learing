# 第七课面试题整理

## 题目 1

**问题：** 为什么说“成功调用一次 LLM”虽然简单，却是所有 Agent 的起点？

**参考答案：** 因为 Agent 的所有上层能力，例如 Tool Calling、Memory、RAG、MCP 和多轮 Loop，本质上都建立在模型调用链路正常的前提上。只有先确认 API Key、SDK、模型接口和 `responses.create()` 都能稳定工作，后续的控制流和工具协作才有意义。换句话说，Agent 不是绕过 LLM，而是在 LLM 之上搭建更复杂的运行时系统。

**追问方向：** 如果第一步连基础模型调用都不稳定，后续开发最容易出现什么问题？

**易错点：** 低估基础链路验证的重要性，急于直接上 Tool 和 Loop。

## 题目 2

**问题：** 为什么 Agent 工程师不应该只关注 `response.output_text`？

**参考答案：** 因为 `response.output_text` 只是最终可读文本的便捷视图，而 Agent 真正需要消费的是 `response.output` 中的结构化输出项。这里面可能包含 `message`、`function_call`、`reasoning` 等不同类型事件。程序必须先识别这些事件类型，才能决定是继续调用工具、继续循环，还是结束任务。因此在 Agent 场景里，`output_text` 更像展示层，而 `output` 才是控制层入口。

**追问方向：** `output_text` 在哪些场景依然有价值？

**易错点：** 把 Agent 返回值当成普通聊天返回的纯文本处理。

## 题目 3

**问题：** 为什么说现代 Agent 返回的不是一句回复，而是一系列 Event？

**参考答案：** 因为现代 Agent 运行过程并不总是“用户提问，模型直接回答”。模型在不同阶段可能返回推理片段、工具调用、普通消息等多种输出类型，这些都可以视为不同事件。Agent 程序的职责就是消费这些事件，并根据事件类型推进状态流转。所以更准确的说法不是“GPT 回复了一句话”，而是“GPT 返回了一组事件，程序据此继续执行”。

**追问方向：** 如果一个 Response 同时包含多个不同类型事件，程序应该如何组织处理逻辑？

**易错点：** 仍然用单轮聊天模型的思维理解 Agent 返回结构。

## 题目 4

**问题：** 为什么真正决定 Agent 的不是 `client.responses.create()`，而是 `while True`？

**参考答案：** 因为 `client.responses.create()` 只是发起一次模型请求的接口调用，它解决的是“如何访问模型”。而 Agent 的关键在于如何围绕模型请求形成持续闭环，即收到响应后判断是否需要调用工具、执行工具、回传结果，再继续下一轮，直到完成任务。这个闭环控制逻辑通常由 `while True` 或等价状态机来承载，所以 Loop 才是 Agent 的核心。

**追问方向：** 在什么情况下，`while True` 会进一步演化成更明确的状态机或图结构？

**易错点：** 把“能调模型”误认为“已经实现 Agent”。

## 题目 5

**问题：** 为什么说 Agent 更准确地理解为 State Machine，而不只是 Tool Calling？

**参考答案：** 因为 Tool Calling 只是 Agent 运行过程中的一个动作分支，而 Agent 整体上要经历多个状态的流转，例如开始、调用模型、接收响应、判断是否为 Tool Call、执行工具、再次调用模型、最终结束。这种“状态之间按条件转移”的结构，本质上就是状态机。很多成熟框架之所以引入 Graph、Runner、Session 等抽象，也是为了更优雅地组织这些状态流转，而不仅仅是执行某个工具函数。

**追问方向：** State Machine 视角相比“循环调用模型”视角，多解决了什么问题？

**易错点：** 认为 Agent 只要有 Tool Calling 就够了，没有意识到状态管理才是框架设计核心。

## 题目 6

**问题：** 为什么后续学习路线会强调 `Responses API + Agents SDK + MCP`，而不是一开始就围绕 LangChain？

**参考答案：** 因为这条路线更接近现代 Agent 工程的主流底层接口和抽象方式。`Responses API` 提供结构化事件输出，`Agents SDK` 提供更贴近运行时的 Agent 组织方式，`MCP` 提供更标准化的工具和上下文协议。相比之下，LangChain 更像高层封装框架，适合在理解底层之后使用。如果一开始就只学封装层，很容易会用却不理解为什么框架要这样设计。

**追问方向：** 学完底层后再回头看 LangChain，理解会发生什么变化？

**易错点：** 把“不优先学 LangChain”误解成“LangChain 没有价值”。

