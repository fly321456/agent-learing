# 第一课面试题整理

## 题目 1

**问题：** 为什么 ChatGPT 本身通常不能算一个完整的 Agent？

**参考答案：** 因为 ChatGPT 默认更像一次性推理系统，通常是“用户输入一次，模型回答一次，然后结束”，本质偏向 `Single-shot`。而完整 Agent 是闭环决策系统，具备“思考 - 调用工具 - 获取观察结果 - 重新思考 - 再行动”的循环过程，也就是 `Closed-loop`。所以是否是 Agent，关键不只是是否使用大模型，而是是否具备持续决策与行动闭环。

**追问方向：** Single-shot 和 Closed-loop 的结构差异是什么？

**易错点：** 只回答“因为它不能调用工具”，但没有指出“闭环决策”才是更本质的区别。

## 题目 2

**问题：** Agent 和传统 Workflow 的最大区别是什么？

**参考答案：** Workflow 的流程通常由人预先设计好，属于 `Human Designed`，步骤固定、顺序固定，面对不同输入也往往走同一套路径。Agent 则更接近 `Model Designed`，由模型根据任务目标和当前观察结果动态决定下一步动作，因此流程是不固定的。两者最大的区别在于：Workflow 是静态编排，Agent 是动态规划。

**追问方向：** 什么场景更适合 Workflow，什么场景更适合 Agent？

**易错点：** 只说“Agent 更灵活”，但没有明确指出“谁在决定流程”。

## 题目 3

**问题：** 为什么说 Tool 决定了 Agent 的能力边界？

**参考答案：** 因为 LLM 主要负责推理、判断和决策，即决定“做什么”；真正执行外部动作的是 Tool，例如读文件、联网搜索、执行代码、调用数据库、操作浏览器等。如果没有相应 Tool，即使模型知道下一步应该怎么做，也无法真正落地执行。因此 Agent 的能力边界，本质上取决于它可调用的工具集合及工具质量。

**追问方向：** 如果模型足够强，是否可以替代 Tool？

**易错点：** 只说“Tool 很重要”，但没有讲清楚 LLM 和 Tool 的职责分工。

## 题目 4

**问题：** 设计一个“自动分析 GitHub 项目”的 Agent，至少需要哪些 Tool？

**参考答案：** 至少需要具备仓库获取、文件读取、文件搜索、命令执行和结果总结等能力。对应工具可以包括：`Git Clone`、`Read File`、`Search File`、`Execute Shell`、`Execute Python`、`Terminal`，以及视场景扩展的 `Browser Search`、`Markdown Parser`、`PDF Reader`、`Memory` 等。核心原则不是工具名本身，而是这些基础能力必须存在，否则 Agent 无法完成真实项目分析任务。

**追问方向：** 如果项目是 Java、Python、前端项目，各自还需要补什么能力？

**易错点：** 只列出“联网、下载、读文件”这类过于粗粒度的能力，没有覆盖搜索、执行和记忆等关键环节。

## 题目 5

**问题：** Agent 执行过程中为什么 `Observe` 很重要？

**参考答案：** 因为 Agent 不是预先把所有步骤想完再执行，而是每执行一次动作，就会获得新的外部信息。`Observe` 阶段负责接收这些信息，例如文件内容、命令输出、网页结果、报错信息等。模型会根据这些观察结果重新判断下一步，因此 `Observe` 是让 Agent 具备动态调整能力的核心环节。没有 Observe，Agent 很容易退化成固定流程或伪循环。

**追问方向：** `Think - Act - Observe` 和 `Observe - Reason - Act` 有什么关系？

**易错点：** 只强调 Think 和 Act，忽略工具结果会反过来改变推理过程。

