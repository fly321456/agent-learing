# 第十一课面试题整理

## 题目 1

**问题：** 为什么在 Agent 学习路径上，不建议一开始就先学 LangChain？

**参考答案：** 因为 LangChain 这类高层框架对 Tool Calling、Loop、状态管理和上下文拼接做了大量封装，初学者虽然可以快速跑通示例，但往往不清楚内部到底发生了什么。这样容易形成“会调用框架 API，但不理解 Agent 运行原理”的状态。更推荐的路线是先掌握 OpenAI SDK、Responses API 和官方 Agents SDK 的设计，再回头看高层框架，这样更容易理解每一层抽象的职责。

**追问方向：** 学完底层再看 LangChain，会在哪些地方理解得更透？

**易错点：** 把“不先学 LangChain”误解成“LangChain 不重要”。

## 题目 2

**问题：** 为什么说 OpenAI Agents SDK 中的 `Agent` 更像 Configuration，而不是一个会自动运行的机器人？

**参考答案：** 因为 `Agent` 主要保存的是静态能力定义，例如 Prompt、Model、Tools 和 Settings，它描述的是“这个智能体是谁、能做什么、应该按什么边界工作”。它本身并不负责驱动循环、管理生命周期或执行工具，因此更像配置对象，而不是完整运行时。真正让配置运行起来的是 `Runner` 这类运行时组件。

**追问方向：** 如果把运行逻辑强行塞回 Agent，会带来什么设计问题？

**易错点：** 看到 `Agent()` 就默认它已经具备完整执行能力。

## 题目 3

**问题：** 为什么官方框架要设计 `Runner.run(agent, input)`，而不是简单地提供 `agent.run()`？

**参考答案：** 因为 `Agent` 和 `Runner` 承担的是不同职责。Agent 负责能力描述和配置，Runner 负责运行过程、状态流转、生命周期控制和 Agent Loop。将二者拆开符合职责分离原则，也让同一个 Runner 能运行不同 Agent，甚至支持 Multi-Agent 编排。这个设计比把所有逻辑揉进 `agent.run()` 更利于复用、扩展和维护。

**追问方向：** 如果未来需要支持会话恢复、Tracing 或并发运行，Runner 会承担哪些额外职责？

**易错点：** 把 `Runner` 视为只是一个多余的包装层。

## 题目 4

**问题：** `@function_tool` 这类机制真正解决的是什么问题？

**参考答案：** 它解决的不是“让 Python 能调用函数”，因为 Python 本来就能调用普通函数；它真正解决的是“让 LLM 能理解并正确使用这个函数”。通过 `@function_tool` 之类的机制，框架可以自动从函数提取名称、描述、参数结构等信息，生成 Tool Schema，再把这个 Schema 提供给模型。这样模型才能知道什么时候该调用这个工具，以及调用时应传什么参数。

**追问方向：** 如果没有这层工具包装，只把函数对象直接传给模型会怎样？

**易错点：** 误以为 function_tool 的作用只是语法糖或注册函数而已。

## 题目 5

**问题：** 为什么本周最重要的学习目标不是 MCP、多 Agent 或 RAG，而是先吃透那五个基础问题？

**参考答案：** 因为 MCP、Memory、多 Agent、RAG 这些高级能力，本质上都是在已有 Agent 框架骨架上继续扩展。如果对“Agent 是配置还是运行时”“为什么需要 Tool Schema”“Loop 为什么是状态机”“为什么要有 LLM Interface”“为什么要用 Responses API”这些基础问题理解不清，后续新能力只会越学越乱。先把框架骨架打牢，后面增加能力才是自然扩展，而不是不断打补丁。

**追问方向：** 如果基础骨架没打稳，后面学高级能力时最容易出现什么错觉？

**易错点：** 追着新概念跑，但没有稳定的底层心智模型。

## 题目 6

**问题：** 为什么课程从这一课开始切换到 Pair Programming + Git Commit 驱动的节奏？

**参考答案：** 因为这种方式更接近真实工程团队培养初级工程师的模式。每一节课完成一个真实 Commit，意味着每次学习都有明确边界、可回溯代码变更和可复盘的架构决策，不再只是停留在“听懂了”。这种节奏能让学习者逐步建立项目演进能力、代码评审意识和工程交付习惯，最终形成一个完整的 Agent Framework 项目，而不是一堆零散知识点。

**追问方向：** 这种推进方式对面试、作品集和源码理解分别有什么帮助？

**易错点：** 把结对编程和提交节奏只看成形式，没有看到它对能力成长路径的影响。

