# 第二课面试题整理

## 题目 1

**问题：** 为什么说 Tool Calling 的本质就是一段 JSON？

**参考答案：** 因为在 Tool Calling 场景里，LLM 并不会直接执行工具，而是输出一段结构化的调用意图，里面说明“该调用哪个工具”以及“需要传入什么参数”。这种结构化数据通常可以抽象理解为 JSON，例如包含 `tool` 和 `arguments` 两部分。它的本质不是代码执行，而是调用决策的结构化表达。

**追问方向：** OpenAI、Claude、Gemini 的 Tool Calling 在底层形式上有什么共同点？

**易错点：** 把 Tool Calling 理解成“模型直接运行函数”，而不是“模型输出调用意图”。

## 题目 2

**问题：** 为什么 LLM 不能直接执行 Python 函数，而必须由我们的程序来执行？

**参考答案：** 因为 LLM 本质上是一个生成下一个 token 的概率模型，它可以根据上下文输出文本或结构化内容，但它本身并不持有真实运行环境，也不会直接操作本地函数、文件系统、网络或操作系统。真正执行 Python 函数需要外部宿主程序完成，也就是由我们的代码接收模型输出的 Tool Call，再映射到具体函数执行，并把结果返回给模型。

**追问方向：** 如果不给外部程序处理 Tool Call，只让模型单独运行，会发生什么？

**易错点：** 把“模型会写出函数名”误解成“模型已经执行了函数”。

## 题目 3

**问题：** 一个 Tool 至少应包含哪些核心组成部分？

**参考答案：** 一个 Tool 至少应包含四部分：`Name`、`Description`、`Parameters` 和实际执行逻辑。Name 用于标识工具；Description 用于告诉模型这个工具适用于什么任务；Parameters 用于约束调用时必须提供哪些参数；执行逻辑则由外部程序真正完成。很多平台还会明确 Return 结构，以便结果更稳定地回传给模型。

**追问方向：** 为什么 Description 往往比函数代码本身更影响调用效果？

**易错点：** 只关注函数实现，忽略工具元信息设计。

## 题目 4

**问题：** 为什么说 Description 比代码还重要？

**参考答案：** 因为模型在决定是否调用某个 Tool 时，首先依赖的是工具的名字、描述和参数定义，而不是去阅读函数实现源码。Description 越准确，模型越容易把用户意图映射到正确工具；描述模糊时，即使函数写得再完整，模型也可能不知道什么时候该调用它。因此在 Agent 系统中，Description 实际上是模型理解工具能力边界的主要依据。

**追问方向：** 一个好的 Description 应该包含哪些信息？

**易错点：** 误以为只要函数能跑，模型自然就会正确调用。

## 题目 5

**问题：** `read_file` Tool 应该如何设计？

**参考答案：** 可以这样设计：Name 为 `read_file`；Description 为“读取指定路径文件的文本内容，适用于查看源码、配置、文档等文件内容”；Parameters 至少包含 `path: string`，必要时还可以扩展编码、行范围等参数；Return 可以设计为读取到的文本内容，或者包含 `content`、`path`、`success`、`error` 等字段的结构化结果。设计重点是让模型清楚知道这个工具能做什么、需要什么输入、会返回什么输出。

**追问方向：** `read_file` 需要考虑哪些安全性和异常处理问题？

**易错点：** 只写 `path` 一个参数和“读取文件”一句描述，没有说明适用范围、返回格式和失败情况。

## 题目 6

**问题：** LLM 是怎么知道该调用哪个 Tool 的？

**参考答案：** 因为开发者会事先把所有 Tool 的名称、描述和参数 schema 提供给模型。模型读到用户请求后，会根据语义匹配判断哪个 Tool 最适合解决当前问题，然后输出对应的 Tool Call。也就是说，模型不是“知道函数实现细节”，而是“根据工具说明书做工具选择”。

**追问方向：** 如果两个 Tool 的 Description 很像，会出现什么问题？

**易错点：** 以为模型是通过真实执行后再反推工具，而不是根据工具定义先做选择。

