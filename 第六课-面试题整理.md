# 第六课面试题整理

## 题目 1

**问题：** 为什么 OpenAI 在 Agent 场景下推出了 `Responses API`，而不是继续只使用 `Chat Completions`？

**参考答案：** 因为 `Chat Completions` 更适合“消息输入、文本输出”的对话模式，主要服务于聊天、问答、翻译、总结等单轮或多轮文本场景。而 Agent 需要处理的不只是文本，还包括 Tool Call、结构化动作、可能的推理片段和多种输出类型。`Responses API` 用统一的响应对象承载这些不同类型的输出，更适合 Agent 所需的事件驱动和多阶段交互流程。

**追问方向：** 如果强行用 Chat Completions 实现复杂 Agent，会在哪些方面变得别扭？

**易错点：** 只回答“Responses API 更新”，没有指出两者在交互模型上的本质差异。

## 题目 2

**问题：** 为什么在 Agent 开发里，`response.output` 往往比 `response.output_text` 更重要？

**参考答案：** 因为 `response.output_text` 主要反映的是模型生成的文本内容，而 Agent 更关心这一轮返回里到底包含了什么类型的输出，例如 `message`、`function_call` 或其他结构化事件。`response.output` 提供的是完整输出项集合，程序可以据此判断当前该结束、该执行工具，还是该继续下一轮。因此在 Agent 场景里，`response.output` 是做流程控制的关键入口。

**追问方向：** 在什么场景下只看 `output_text` 就足够了？

**易错点：** 把 Agent 返回值仍然当成单纯字符串处理。

## 题目 3

**问题：** 为什么说现代 Agent 更像“事件驱动系统”，而不是“文本驱动系统”？

**参考答案：** 因为 Agent 运行时并不是只消费一段最终文本，而是要根据模型返回的不同事件类型来决定程序下一步动作。例如返回 `function_call` 时要执行 Tool，返回普通 `message` 时可能表示进入总结阶段，未来还可能有其他动作类型。程序的主逻辑因此不再围绕“文本是什么”，而是围绕“这一轮发生了什么事件”展开，这就是典型的事件驱动模式。

**追问方向：** 事件驱动和状态机驱动在 Agent 里有什么关系？

**易错点：** 认为 Agent 只是“更复杂一点的聊天机器人”。

## 题目 4

**问题：** 为什么真正的 Agent 代码常常会写成 `for item in response.output:`？

**参考答案：** 因为 `response.output` 代表这一轮模型返回的完整结构化输出项集合，而 Agent 需要逐个检查这些输出项的类型，再决定如何处理。例如遇到 `function_call` 就执行工具，遇到普通消息就提取文本内容，遇到其他类型则交给对应分支处理。这个写法体现的是对结构化事件流的遍历和分派，而不是单纯读取一个字符串字段。

**追问方向：** 如果一轮 response 里同时包含多个 item，程序应该怎么设计？

**易错点：** 假设每次返回永远只有一段文本。

## 题目 5

**问题：** 为什么普通聊天机器人通常只需要 `print(response.output_text)`，而 Agent 必须先看 `response.output`？

**参考答案：** 因为普通聊天机器人的核心任务是获取模型生成的自然语言文本，所以只关心最终文本输出即可。而 Agent 的任务不仅是显示答案，还要根据模型输出的结构化事件来驱动工具调用和循环控制。如果只看 `output_text`，就可能错过 `function_call` 等关键信号，从而无法让程序继续执行后续步骤。因此聊天机器人消费的是文本，Agent 消费的是事件。

**追问方向：** 如果只打印 `output_text`，在 Tool Calling 场景下可能会遗漏哪些信息？

**易错点：** 把两类程序的输出消费方式混为一谈。

## 题目 6

**问题：** 第六课对 Agent 开发者最关键的思维升级是什么？

**参考答案：** 最关键的升级是从“文本输出思维”切换到“结构化事件思维”。也就是不再默认模型返回的就是最终文本，而是先判断这一轮返回里包含哪些事件类型，再决定程序如何响应。这个转变直接决定了开发者能否真正理解 Responses API、Tool Calling 和 Agent Loop 的设计方式，也是从普通 LLM 应用开发迈向 Agent 工程开发的关键门槛。

**追问方向：** 这种思维升级会如何影响后续代码结构设计？

**易错点：** 只记住 API 名字变化，没有建立新的程序模型。

